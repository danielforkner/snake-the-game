important!: 
- fix button layout for mobile
- can you find any loop that might be causing the game to crash seemingly randomly?

spells

replace switch statements with string literals like characters[`boss${gameState.currentBoss}`]. IF DOESNT WORK ASK QUESTION


   applePlenty: {
        className: 'makeApples'
    },... add css class for the buff. make the buff change apple_multiplyer to 10. make an item image.